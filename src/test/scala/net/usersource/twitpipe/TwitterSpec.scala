package net.usersource.twitpipe

import org.scalatest.matchers.MustMatchers
import org.scalatest.{FeatureSpec, GivenWhenThen}

class TwitterSpec extends FeatureSpec with GivenWhenThen with MustMatchers {

  feature("creation") {
    scenario("when ") {
      given("")
      when("")
      then("")
    }
  }
}